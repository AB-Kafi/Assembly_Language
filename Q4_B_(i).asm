

;        ansert to the question Q4b
;        
;
;
;       SHL is Shift instruction , it shifts a bit 1 index to it's left
;       ROL is Rotate isntruction , it rotate all the bit's to left given index.doesn't move carry
;       RCL is same as ROL but it moves carry bit as well.